struct UInt8Vector {
    var ptr: UnsafePointer<UInt8>
    var length: UInt64
    var capacity: UInt64

    init() {
        self.capacity = 0
        self.ptr = malloc(0)
        self.length = 0
    }
}
